Получение списка статей
---
tags:
  - Articles
definitions:
  ArticleList:
      type: array
      items:
        $ref: '#/definitions/Article'
responses:
  200:
    description: Успех
    schema:
      $ref: '#/definitions/ArticleList'